interface:
  display_name: "OpenSpec Propose"
  short_description: "Create proposal, design, and tasks"
  default_prompt: "Use $openspec-propose to define this change and generate the OpenSpec artifacts needed before implementation."

